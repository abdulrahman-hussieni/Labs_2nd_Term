
# توليد النغمات الصوتية باستخدام MATLAB/Octave

يوضح هذا المشروع كيفية إنشاء **نغمة صوتية نقية (Pure Tone)** برمجياً عن طريق توليد موجة جيبية (Cosine Wave) وتحويلها إلى إشارة صوتية مسموعة.

## الكود المستخدم (The Code)

```matlab
% إعدادات المعاملات
% Parameter setup
fs = 8000;              % Sampling Frequency (Hz)
time = 1;               % Duration (seconds)
f0 = 500;               % Tone frequency (Hz)

% Time vector
t = 0:(1/fs):time;

% Generate cosine wave
% 0.1 is used to reduce amplitude and avoid clipping
x = 0.1 * cos(2 * pi * f0 * t);

% Play sound
sound(x, fs, 16);

```

---

## شرح المكونات التقنية

### 1. لماذا اخترنا الحدود من 0 إلى 1؟

تمثل هذه الحدود **الفترة الزمنية** للإشارة.

* الرقم `0` هو لحظة البداية.
* الرقم `1` هو لحظة النهاية (ثانية واحدة).
* **الدقة:** بما أن التردد `fs` هو 8000، فإن البرنامج يقوم بإنشاء 8000 نقطة حسابية داخل هذه الثانية الواحدة لضمان سلاسة الصوت عند تحويله من صيغة رقمية إلى تماثلية (Digital to Analog).

### 2. لماذا دالة جيب التمام `cos`؟

تعتبر دالة `cos` (أو `sin`) هي الوحدة البنائية الأساسية للصوت في الفيزياء:

* **النقاء:** تنتج دالة الكوساين ما يسمى بالـ "Simple Harmonic Motion"، وهو ما نسمعه كـ "نغمة نقية" بدون ضجيج.
* **التردد:** المعادلة المستخدمة هي ، حيث  هو التردد الذي يحدد حدة الصوت (Pitch). كلما زاد هذا الرقم، أصبح الصوت "أرفع" أو أكثر حدة.

### 3. سعة الصوت (Amplitude)

لاحظ استخدام معامل الضرب `0.1`. هذا المعامل يتحكم في **شدة الصوت**:

* القيم الافتراضية لـ `cos` تتراوح بين  و .
* استخدام القيمة القصوى () قد يؤدي إلى ظاهرة الـ **Clipping** (تشوه الصوت) إذا كانت سماعات الجهاز غير مهيأة، لذا نستخدم  كقيمة آمنة ومريحة للأذن.

---
📐 Nyquist Criterion (The Golden Rule)

To reconstruct a signal correctly:

fs > 2 * f0

Why?
✔ Prevent Aliasing

If sampling frequency is too low, the signal becomes distorted.

✔ Perfect Reconstruction

Enough samples ensure accurate waveform rebuilding..


انشاء موجة مركبة 
```matlab
 % x = A*cos(2*pi*f0*t)

Fs = 8000;
time = 1;
t = 0:(1/Fs):time;

x = 0.1*cos(2*pi*300*t) + 0.1*cos(2*pi*400*t);

sound(x, Fs, 16);


```
رسم الموجة
:

```matlab
% x = A*cos(2*pi*f0*t)

Fs = 8000;
time = 1;

t = 0:(1/Fs):time;

x = 0.1*cos(2*pi*300*t);

sound(x,Fs,16);

figure(1);plot(t,x),grid;
xlabel('Time')
ylabel('Amplitude')
title('Time domain representation of x(t)')
xlim([0,0.1])
```
رسم موجة عشوائية(random signal uniformly distributed):
:

```matlab
Fs = 8000;                 % تردد أخذ العينات (عدد العينات في الثانية)

time = 1;                  % مدة الإشارة بالثواني

N = Fs*time;               % إجمالي عدد العينات = التردد × الزمن

x = rand(1,N)*2-1;         % توليد إشارة عشوائية بين -1 و +1 (ضوضاء بيضاء)

t = linspace(0,time,N);    % إنشاء متجه الزمن من 0 إلى 1 بعدد N من النقاط

sound(x,Fs,16);            % تشغيل الإشارة الصوتية بدقة 16-bit

figure(1);                 % فتح نافذة رسم رقم 1

plot(t,x),grid;            % رسم الإشارة مقابل الزمن مع إظهار الشبكة

xlabel('Time')             % تسمية المحور الأفقي (الزمن)

ylabel('Amplitude')        % تسمية المحور الرأسي (السعة)
```

تسجيل صوت:
*:

```matlab
% Prefered to use Fs = 8000, 11025, 22050, 44100 Hz   % قيم قياسية لتردد أخذ العينات

Fs = 8000;                     % تحديد تردد أخذ العينات (عدد العينات في الثانية)

time = 3;                      % مدة التسجيل بالثواني

recObj = audiorecorder(Fs,16,1); % إنشاء كائن تسجيل (Fs, دقة 16-bit, قناة واحدة Mono)

recordblocking(recObj,time);   % بدء التسجيل وإيقاف تنفيذ البرنامج حتى انتهاء المدة

x = getaudiodata(recObj);      % استخراج البيانات الصوتية المسجلة وتخزينها في المتغير x

play(recObj);                  % تشغيل الصوت الذي تم تسجيله
```

---

:لكود الاتي بيعمل 3 حاجات أساسية:

✅ يسجل صوت من المايك
✅ يشغله تاني
✅ يجهز البيانات عشان ترسمها 

```matlab
% Prefered to use Fs = 8000, 11025, 22050, 44100 Hz   % قيم قياسية لتردد أخذ العينات

Fs = 44100;                     % اختيار تردد عينات عالي (جودة CD تقريباً)

time = 3;                       % مدة التسجيل بالثواني

recObj = audiorecorder(Fs,16,1); % إنشاء كائن تسجيل (Fs ، دقة 16-bit ، قناة واحدة Mono)

recordblocking(recObj,time);    % بدء التسجيل وإيقاف تنفيذ البرنامج حتى انتهاء المدة

x = getaudiodata(recObj);       % استخراج البيانات الصوتية من كائن التسجيل

play(recObj);                   % تشغيل الصوت المسجل

N = length(x);                  % حساب عدد العينات (عدد الـ samples)

t = linspace(0,time,N);         % إنشاء متجه زمن متقطع بنفس عدد العينات
```

تحليل:

🎤 **تسجيل صوت من المايكروفون لمدة 3 ثواني، تشغيله، وتجهيزه للرسم أو التحليل.**

خليني أشرح لك خطوة خطوة:

---

### 1️⃣ تحديد تردد أخذ العينات

```matlab
Fs = 44100;
```

ده تردد أخذ العينات (Sampling Frequency).

📌 44100 Hz يعني:

* بيأخذ 44100 عينة في الثانية
* دي نفس جودة الـ CD تقريبًا
* كل ما Fs يزيد → جودة أعلى وحجم بيانات أكبر

---

### 2️⃣ تحديد مدة التسجيل

```matlab
time = 3;
```

هيسجل لمدة **3 ثواني**.

---

### 3️⃣ إنشاء كائن تسجيل

```matlab
recObj = audiorecorder(Fs,16,1);
```

المعاملات معناها:

* `Fs` → تردد العينات
* `16` → دقة الصوت (16-bit)
* `1` → قناة واحدة (Mono)

---

### 4️⃣ بدء التسجيل

```matlab
recordblocking(recObj,time);
```

📌 يسجل لمدة 3 ثواني
📌 البرنامج "يستنى" لحد ما التسجيل يخلص

---

### 5️⃣ استخراج الإشارة المسجلة

```matlab
x = getaudiodata(recObj);
```

هنا بيحول التسجيل إلى متجه رقمي اسمه `x`
وده يمثل الإشارة الصوتية في الزمن.

---

### 6️⃣ تشغيل التسجيل

```matlab
play(recObj);
```

يشغل الصوت اللي لسه اتسجل.

---

### 7️⃣ حساب عدد العينات

```matlab
N = length(x);
```

بيحسب عدد الـ samples اللي اتسجلت.

بما إن:

```
Fs = 44100
time = 3
```

إذن:

```
N ≈ 44100 × 3 = 132300 عينة
```

---

### 8️⃣ إنشاء محور الزمن

```matlab
t = linspace(0,time,N);
```

بيعمل متجه زمن من 0 لـ 3 ثواني بعدد نقاط = عدد العينات
علشان نقدر نرسم الإشارة مقابل الزمن.

---

# 🎯 الخلاصة

الكود بيعمل 3 حاجات أساسية:

✅ يسجل صوت من المايك
✅ يشغله تاني
✅ يجهز البيانات عشان ترسمها أو تعمل عليها تحليل (FFT مثلاً)

---

---
---
تمام 👌
ا**تخزين الصوت جوه الكمبيوتر** في ملف بصيغة WAV.

السطر المسؤول عن الحفظ هو:

```matlab
audiowrite('test.wav', x, Fs)
```

## 🎯 ده بيعمل إيه؟

* `'test.wav'` → اسم الملف اللي هيتحفظ
* `x` → الإشارة الصوتية المسجلة
* `Fs` → تردد أخذ العينات

📁 الملف هيتحفظ في **نفس فولدر السكربت** (المسار الحالي في MATLAB).

---

# 💾 الكود كامل مع الحفظ

```matlab
% Prefered to use Fs = 8000, 11025, 22050, 44100 Hz

Fs = 44100;                  % تردد أخذ العينات
time = 3;                    % مدة التسجيل

recObj = audiorecorder(Fs,16,1);  % إنشاء كائن تسجيل

recordblocking(recObj,time);      % تسجيل لمدة 3 ثواني

x = getaudiodata(recObj);         % استخراج البيانات الصوتية

play(recObj);                     % تشغيل التسجيل

N = length(x);                    % عدد العينات
t = linspace(0,time,N);           % إنشاء متجه الزمن

audiowrite('test.wav',x,Fs);      % حفظ التسجيل كملف WAV على الكمبيوتر
```

---

# 📌 لو عايز تتحكم في مكان الحفظ

مثلاً تحفظه على الـ Desktop:

```matlab
audiowrite('C:\Users\YourName\Desktop\test.wav', x, Fs);
```

غير `YourName` باسم المستخدم عندك.

---

# 🔥 ملحوظة مهمة

صيغة WAV:

* غير مضغوطة
* جودة عالية
* مناسبة لتحليل DSP

---
قراءة ملف صوتي من الجهاز:

```matlab
[x,Fs] = audioread('Tom.wav');

sound(x,Fs,16)
```

---

## 🎯 الكود ده بيعمل إيه؟

* `audioread('Tom.wav')`
  بيقرأ الملف الصوتي **Tom.wav** من نفس الفولدر.

* `x` → الإشارة الصوتية

* `Fs` → تردد أخذ العينات

* `sound(x,Fs,16)`
  بيشغل الملف بدقة 16-bit.

---

📌 ملحوظة مهمة:
لازم ملف **Tom.wav** يكون موجود في نفس الـ Current Folder في MATLAB
أو تكتب المسار الكامل للملف.
👌
---
🎧 أبسط مثال لقراءة ملف صوت
```matlab
[x, Fs] = audioread('test.wav');   % قراءة الملف الصوتي

sound(x, Fs);                      % تشغيل الصوت

N = length(x);                     % عدد العينات
t = linspace(0, N/Fs, N);          % إنشاء محور الزمن

figure;
plot(t, x);
grid;
xlabel('Time (seconds)');
ylabel('Amplitude');
title('Time Domain Representation');

📌 شرح سريع

x → الإشارة الصوتية

Fs → تردد أخذ العينات

sound(x,Fs) → تشغيل الملف

plot(t,x) → رسم الإشارة
```
---
---
سؤال ممتاز 👌
خلينا نفهم التغيير ده بيأثر على إيه بالظبط 👇

السطر اللي اتغير هو:

```matlab
sound(x, 0.8*Fs, 16)
```

بدل ما كان:

```matlab
sound(x, Fs, 16)
```

---

# 🎯 ده بيأثر على إيه؟

إنت غيرت **سرعة التشغيل (Playback Speed)**
مش الإشارة نفسها.

يعني:

* `x` لم تتغير
* عدد العينات لم يتغير
* لكن معدل تشغيل العينات اتغير

---

# 📉 لما تستخدم 0.8 * Fs

يعني شغلت الصوت بسرعة أقل من الأصل.

## النتيجة:

✅ الزمن يزيد
✅ التردد يقل (الصوت يبقى أغلظ)
✅ السرعة تقل

---

# 🧠 ليه ده بيحصل؟

مدة الإشارة بتتحسب كده:

[
\text{Time} = \frac{N}{Fs}
]

لو قللت Fs أثناء التشغيل:

[
\text{Time الجديدة} = \frac{N}{0.8Fs}
]

وده أكبر من الزمن الأصلي.

---

# 📊 مثال رقمي

لو الملف مدته الأصلية:

3 ثواني

عند تشغيله بـ 0.8Fs:

[
\frac{3}{0.8} = 3.75 \text{ ثانية تقريباً}
]

يعني الزمن زاد.

---

# 🔊 طب التردد يحصل له إيه؟

كل الترددات تضرب في 0.8

يعني:

* نغمة 1000 Hz تبقى 800 Hz
* الصوت يبقى أبطأ وأغلظ

---

# 📝 القاعدة العامة

| لو ضربت Fs في | يحصل إيه                             |
| ------------- | ------------------------------------ |
| > 1           | الزمن يقل – الصوت أسرع – التردد أعلى |
| < 1           | الزمن يزيد – الصوت أبطأ – التردد أقل |

---

# 🎯 إجابة سؤالك مباشرة

> المسافة تزيد؟ التردد يزيد؟ الزمن يقل؟

في حالتك (0.8Fs):

❌ التردد لا يزيد
❌ الزمن لا يقل
✅ الزمن يزيد
✅ التردد يقل

---
زي البرامج اللي كنا بنلعب بيها 
-----
----


DSP Lab 5/8 || FFT (Fast Fourier Transform)
-----
ممتاز 👌
خلينا نشرح **FFT (Fast Fourier Transform)** بطريقة واضحة وعميقة كأنك في محاضرة DSP 👇

---

# 🎯 أولاً: الفكرة الأساسية

أي إشارة في الزمن ( x(t) )
ممكن تتفكك لمجموعة ترددات مختلفة.

يعني بدل ما نبص على الإشارة في **Time Domain**
نقدر نشوفها في **Frequency Domain**.

---

# 🧠 مثال بسيط

لو عندك:

[
x(t) = 0.1\cos(2\pi 500 t)
]

دي نغمة واحدة 500 Hz.

في الزمن → شكل موجة
في التردد → خط واحد عند 500 Hz

---

# 📌 ما هو Fourier Transform؟

هو تحويل رياضي يحول:

[
x(t) \rightarrow X(f)
]

يعني من الزمن → إلى التردد

---

# 🚀 طب الـ FFT إيه؟

FFT = Fast Fourier Transform
هو خوارزمية سريعة لحساب الـ DFT (Discrete Fourier Transform).

بدل ما التعقيد يكون:

[
O(N^2)
]

يبقى:

[
O(N \log N)
]

فرق ضخم جدًا لما N يكبر.

---

# 📊 في الكود عندك

```matlab
X_k = abs(fft(x));    % حساب مقدار الـ FFT
f = linspace(0,Fs,N); % إنشاء محور التردد
plot(f,X_k)
```

---

## 🔎 نشرح سطر سطر

### 1️⃣

```matlab
fft(x)
```

تحسب التمثيل الترددي للإشارة.

الناتج أعداد مركبة (complex numbers):

[
X(k) = Real + jImag
]

---

### 2️⃣

```matlab
abs(fft(x))
```

نأخذ المقدار فقط:

[
|X(k)|
]

وده اسمه:

> Amplitude Spectrum

---

### 3️⃣

```matlab
f = linspace(0,Fs,N);
```

بدل ما نرسم مقابل k (مؤشر العينة)

بنرسم مقابل التردد الحقيقي بالهرتز:

[
f = \frac{k \cdot Fs}{N}
]

---

# 🎵 ماذا يظهر في الرسم؟

## حالة الإشارة الجيبية 500 Hz

هتشوف:

* قمة عند 500 Hz
* وقمة تانية عند ( Fs - 500 )

ليه؟

لأن FFT لإشارة حقيقية بيكون متماثل.

---

## حالة الإشارة العشوائية

هتشوف:

* توزيع ترددات في كل الطيف
* لأن الضوضاء البيضاء تحتوي على كل الترددات

---

# 📐 العلاقة المهمة جداً

تردد كل عنصر في FFT هو:

[
f_k = \frac{k Fs}{N}
]

حيث:

* k من 0 إلى N-1
* Fs تردد العينات
* N عدد العينات

---

# ⚠️ نقطة مهمة جداً

الـ FFT يطلع ترددات من:

[
0 \rightarrow Fs
]

لكن في الحقيقة:

المعلومات الفعلية المفيدة من:

[
0 \rightarrow \frac{Fs}{2}
]

وده بسبب:

> Nyquist Theorem

---

# 🎯 لماذا Fs/2 فقط؟

لأن:

[
f_s > 2 f_{max}
]

إذن أعلى تردد حقيقي موجود هو:

[
\frac{Fs}{2}
]

---

# 🧠 الفكرة العميقة

الإشارة في الزمن
هي جمع جيوب:

[
x(t) = \sum A_k \cos(2\pi f_k t)
]

FFT يكشف:

* ما هي الترددات الموجودة؟
* ما سعة كل تردد؟

---

# 📊 الفرق بين Time & Frequency Domain

| المجال الزمني        | المجال الترددي |
| -------------------- | -------------- |
| شكل الموجة           | مكونات التردد  |
| صعب تعرف التردد بدقة | سهل تعرف القمم |
| مناسب للرؤية العامة  | مناسب للتحليل  |

---

# 🔥 مثال عملي

لو عندك:

```matlab
x = cos(2*pi*300*t) + cos(2*pi*400*t);
```

الـ FFT هيرسم:

* قمة عند 300 Hz
* قمة عند 400 Hz

---

# 🏁 الخلاصة

FFT هو:

✔ أداة تحليل التردد
✔ يحول الزمن → تردد
✔ يكشف مكونات الإشارة
✔ أساسي في DSP
✔ يستخدم في الصوت – الاتصالات – الصور – الرادار – كل شيء تقريبًا

---

لو تحب أشرحلك:

* الفرق بين DFT و FFT رياضيًا
* ليه الطيف متماثل
* إزاي نرسم نصف الطيف الصحيح عمليًا
* إزاي نحسب التردد المسيطر من FFT

---
أكيد 👌
ده الكود كامل مكتوب زي الصورة:

```matlab
close all; clear; clc;

% Prefered to use Fs = 8000, 11025, 22050, 44100 Hz

Fs = 44100;
time = 3;

recObj = audiorecorder(Fs,16,1);

recordblocking(recObj,time);     % stop prog. & record

x = getaudiodata(recObj);

play(recObj);

N = length(x);                   % #samples
t = linspace(0,time,N);          % discretize time

figure(1); plot(t,x), grid;
xlabel('Time')
ylabel('Amplitude')
title('Time domain representation of x(t)')
xlim([0,0.1])

X_k = abs(fft(x));               % calculate absolute of fft
f = linspace(0,Fs,N);            % discretize frequency

figure(2); plot(f,X_k), grid;
title('Amplitude Spectrum of x(t)')
xlabel('Frequency (Hz)')
ylabel('|X(f)|')
```

---

# 🎯 الكود ده بيعمل ايه؟

الكود بيعمل 3 مراحل رئيسية:

---

# 1️⃣ تسجيل الصوت من المايك

* يسجل لمدة 3 ثواني
* بجودة 44100 Hz
* بدقة 16-bit
* قناة واحدة (Mono)

ويخزن الصوت في المتغير `x`

---

# 2️⃣ رسم الإشارة في المجال الزمني (Time Domain)

```matlab
plot(t,x)
```

ده بيوريك شكل الصوت مقابل الزمن
يعني تشوف الموجة نفسها.

و:

```matlab
xlim([0,0.1])
```

بيخليك تشوف أول 0.1 ثانية بس علشان تبان التفاصيل.

---

# 3️⃣ حساب ورسم الطيف الترددي (Frequency Domain)

```matlab
X_k = abs(fft(x));
```

ده بيحسب:

🔹 FFT
🔹 ياخد القيمة المطلقة
🔹 يطلع Amplitude Spectrum

وبعدين:

```matlab
plot(f,X_k)
```

بيرسم الطيف مقابل التردد بالهرتز.

---

# 🧠 الفكرة المهمة هنا

أنت دلوقتي بتشوف الصوت في شكلين:

| الشكل            | معناه                      |
| ---------------- | -------------------------- |
| Time Domain      | شكل الموجة                 |
| Frequency Domain | الترددات الموجودة في الصوت |

---

# 🎵 لو قلت "آآآ" في المايك مثلاً

في المجال الزمني → موجة معقدة
في المجال الترددي → هتشوف قمم عند ترددات معينة (Formants)

---

# 🔥 ده كود كامل لتحليل صوت حقيقي

يسجل
يشغل
يرسم الزمن
يحسب FFT
يرسم الطيف


---
<img width="1435" height="916" alt="image" src="https://github.com/user-attachments/assets/d7fe32d4-b8a7-4f6e-9f52-f237dff11dc3" />

---
ممتاز 👌
ده جزء مهم جدًا في DSP — خلينا نشرحه **بعمق ومنظم خطوة خطوة**.

---

# 🔵 أولاً: ما هو FIR ؟

FIR = **Finite Impulse Response**

يعني:

> استجابة النبضة تنتهي بعد عدد محدود من العينات.

المعادلة العامة:

[
y[n] = \sum_{k=0}^{N} b_k x[n-k]
]

مفيش feedback
مفيش اعتماد على y[n-1]

---

# 🔵 ليه FIR دايمًا Stable ؟

الاستقرار في الأنظمة LTI معناه:

> لو دخل محدود → يطلع خرج محدود
> (BIBO Stability)

شرط الاستقرار:

[
\sum |h[n]| < \infty
]

في FIR:

* عدد معاملات b محدود
* h[n] = b[n]
* عددهم finite

إذن المجموع محدود دائمًا

✅ إذن FIR دايمًا Stable

---

# 🔵 الكود الأول: Moving Average Filter

```matlab
a = 1;
b = 0.25*[1 1 1 1];
```

المعادلة:

[
y[n] = \frac{1}{4}(x[n] + x[n-1] + x[n-2] + x[n-3])
]

ده:

✔ Low Pass Filter
✔ Moving Average
✔ Order = 3
✔ Stable

---

# 🔵 المثال الثاني: High Pass

```matlab
b = 0.25*[1 -1 1 -1];
```

الإشارات المتناوبة تلغي الترددات المنخفضة
فيمرر العالي.

---

# 🔵 تصميم فلتر باستخدام fir1

```matlab
Fs = 8000;
n = 5;
fc = 3000;
b = fir1(n, fc/(Fs/2), 'low');
```

---

## نشرحها بعمق 👇

### 1️⃣ n = 5

ده Order

عدد المعاملات = n+1 = 6

---

### 2️⃣ fc/(Fs/2)

ليه بنقسم على Fs/2؟

لأن MATLAB بيستخدم:

Normalized Frequency

[
\omega = \frac{f}{Fs/2}
]

Fs/2 = Nyquist frequency

---

### 3️⃣ fir1

بيستخدم Window Method لتصميم FIR

بيعتمد على:

Ideal sinc function
×
Window (Hamming default)

---

# 🔵 ليه ظهرلك معاملات زي:

```
-0.0039  -0.0323  0.5362  0.5362  -0.0323  -0.0039
```

دي معاملات الفلتر b

لاحظ:

✔ Symmetric
✔ Linear Phase
✔ FIR
✔ Stable

التماثل ده مهم جدًا:

[
b[k] = b[N-k]
]

ده بيضمن:

✔ No phase distortion
✔ Linear phase

---

# 🔵 لما غيرت n من 5 إلى 50

لاحظ الرسم 👇

✔ الانتقال بقى sharper
✔ الحافة عند 3000 Hz بقت حادة
✔ الاستجابة أقرب للمثالية

ليه؟

كل ما زاد order:

* يزيد عدد taps
* يزيد دقة الفصل
* يقل Transition Band
* يزيد الحسابات

---

# 🔵 تفسير الرسم

في الرسم:

من 0 → 3000 Hz
الـ |H| ≈ 1
ده Passband

بعد 3000 Hz
ينخفض بسرعة للصفر
ده Stopband

---

# 🔵 الفرق بين FIR و IIR

| FIR               | IIR           |
| ----------------- | ------------- |
| دايمًا stable     | ممكن unstable |
| مفيش feedback     | فيه feedback  |
| Linear phase سهل  | صعب           |
| Order كبير غالبًا | Order صغير    |

---

# 🔵 ملخص الجزء كله

✔ FIR = finite coefficients
✔ FIR دايمًا stable
✔ fir1 يصمم فلتر باستخدام window
✔ زيادة n = sharper cutoff
✔ fc لازم تتقسم على Fs/2
✔ المعاملات المتماثلة = linear phase

---

# 🔵 ليه FIR دايمًا Stable رياضيًا؟

لأن:

[
H(z) = b_0 + b_1 z^{-1} + ... + b_N z^{-N}
]

مفيش denominator غير 1

يعني مفيش poles

مفيش poles → مفيش instability

---

لو تحب أدخلك مستوى أعمق ونشرح:

* علاقة zeros بالاستجابة
* إزاي نرسم pole-zero plot
* الفرق الرياضي الكامل بين FIR و IIR
* ليه زيادة n تقلل ripple

----
----
---
تمام 👌
هكتب لك كل الأكواد اللي ظهرت في الجزء ده بشكل منظم وواضح.

---

# 🔵 1️⃣ Moving Average FIR (Low Pass – Order 3)

```matlab
clear; clc; close all;

a = 1;
b = 0.25 * [1 1 1 1];   % Moving average coefficients

figure;
impz(b, a, 10);         % Impulse response

Fs = 8000;
f = (0:0.001:1) * Fs/2; % Physical frequency axis

H = freqz(b, a, f, Fs);

figure;
plot(f, abs(H)), grid;
xlabel('Physical Frequency f (Hz)')
ylabel('Frequency Response |H|')
title('Magnitude Response of Moving Average LPF')
```

---

# 🔵 2️⃣ High Pass FIR (Order 3)

```matlab
clear; clc; close all;

a = 1;
b = 0.25 * [1 -1 1 -1];   % High-pass coefficients

figure;
impz(b, a, 10);

Fs = 8000;
f = (0:0.001:1) * Fs/2;

H = freqz(b, a, f, Fs);

figure;
plot(f, abs(H)), grid;
xlabel('Physical Frequency f (Hz)')
ylabel('Frequency Response |H|')
title('Magnitude Response of High Pass FIR')
```

---

# 🔵 3️⃣ FIR Low Pass باستخدام fir1 (Order = 5)

```matlab
clear; clc; close all;

Fs = 8000;

n  = 5;         % Filter order
fc = 3000;      % Cutoff frequency

a = 1;
b = fir1(n, fc/(Fs/2), 'low');

disp('Filter Coefficients b = ')
disp(b)

figure;
impz(b, a, 20);

f = (0:0.001:1) * Fs/2;
H = freqz(b, a, f, Fs);

figure;
plot(f, abs(H)), grid;
xlabel('Physical Frequency f (Hz)')
ylabel('Frequency Response |H|')
title('FIR Low Pass (Order 5)')
```

---

# 🔵 4️⃣ نفس الفلتر لكن Order = 50 (Sharper Cutoff)

```matlab
clear; clc; close all;

Fs = 8000;

n  = 50;        % Higher order
fc = 3000;

a = 1;
b = fir1(n, fc/(Fs/2), 'low');

figure;
impz(b, a, 60);

f = (0:0.001:1) * Fs/2;
H = freqz(b, a, f, Fs);

figure;
plot(f, abs(H)), grid;
xlabel('Physical Frequency f (Hz)')
ylabel('Frequency Response |H|')
title('FIR Low Pass (Order 50)')
```

---

# 🔵 5️⃣ أمثلة أنواع تانية من fir1

## High Pass

```matlab
b = fir1(n, fc/(Fs/2), 'high');
```

## Bandpass

```matlab
b = fir1(n, [500 1500]/(Fs/2), 'bandpass');
```

## Bandstop

```matlab
b = fir1(n, [500 1500]/(Fs/2), 'stop');
```

---
ممتاز 👌
خلينا نشرح **IIR Filters (Causal LTI)** بشكل عميق ومنظم، ونجاوب أهم سؤال:

> هل IIR دايمًا Stable ؟ ❌
> لا — ممكن يكون Stable أو Unstable حسب مكان الـ Poles.

---

# 🔵 أولاً: ما هو IIR ؟

IIR = Infinite Impulse Response

المعادلة العامة:

[
y[n] = \sum_{k=0}^{M} b_k x[n-k] - \sum_{k=1}^{N} a_k y[n-k]
]

لاحظ:

✔ فيه feedback
✔ فيه معاملات a_k غير صفر
✔ الاستجابة لا تنتهي بعد عدد محدود من العينات

---

# 🔵 مثال 1 من الصورة

[
y[n] = x[n] + 0.5 y[n-1]
]

الكود:

```matlab
clear; clc; close all

Fs = 8000;

b = 1;
a = [1 -0.5];

figure; impz(b,a);
f = (0:0.001:1)*Fs/2;
H = freqz(b,a,f,Fs);
figure; plot(f,abs(H)), grid;
```

---

## 🔎 تحليل الاستقرار

الـ Transfer Function:

[
H(z) = \frac{1}{1 - 0.5z^{-1}}
]

الـ pole عند:

[
z = 0.5
]

|0.5| < 1

✅ إذن Stable

---

# 🔵 مثال 2

[
y[n] = x[n] - 0.9 y[n-1]
]

الكود:

```matlab
b = 1;
a = [1 0.9];
```

الـ pole عند:

[
z = -0.9
]

|-0.9| < 1

✅ Stable

---

# 🔵 القاعدة الذهبية لاستقرار IIR

النظام Stable إذا:

[
\text{All poles inside unit circle}
]

يعني:

[
|z_p| < 1
]

---

# 🔵 لو كان:

[
y[n] = x[n] + 1.2 y[n-1]
]

يبقى pole = 1.2

|1.2| > 1

❌ Unstable

الخرج هيكبر بلا حدود.

---

# 🔵 Butterworth IIR

الكود:

```matlab
clear; clc; close all

Fs = 8000;
n = 5;          % filter order
fc = 3000;      % cutoff frequency

[b,a] = butter(n, fc/(Fs/2), 'low');

figure; impz(b,a);

f = (0:0.001:1)*Fs/2;
H = freqz(b,a,f,Fs);

figure; plot(f,abs(H)), grid;
```

---

## 🔎 ليه Butterworth Stable؟

لأن MATLAB يصممه بحيث:

✔ كل الـ poles داخل unit circle
✔ إذن Stable

---

# 🔵 الفرق بين FIR و IIR في الاستقرار

| FIR           | IIR                     |
| ------------- | ----------------------- |
| دايمًا Stable | ممكن Stable أو Unstable |
| مفيش Poles    | فيه Poles               |
| مفيش feedback | فيه feedback            |

---

# 🔵 الفرق في الاستجابة

FIR:

* Impulse response ينتهي
* Linear phase سهل

IIR:

* Impulse response لا ينتهي
* Phase nonlinear غالبًا
* Order أقل لنفس الأداء

---

# 🔵 ليه IIR أقوى؟

عشان:

بنفس order صغير
يديك Transition sharper من FIR

لكن:

⚠️ ممكن يبقى unstable
⚠️ حساس للأخطاء العددية

---

# 🔵 رياضيًا

[
H(z) = \frac{B(z)}{A(z)}
]

الاستقرار يعتمد على جذور:

[
A(z) = 0
]

دي اسمها Poles

---

# 🔵 ازاي تتأكد من الاستقرار في MATLAB

```matlab
zplane(b,a)
```

لو كل النقاط (×) داخل الدائرة
→ Stable

---

# 🔵 الخلاصة المهمة جدًا

✔ FIR دايمًا Stable
✔ IIR Stable إذا كل poles داخل unit circle
✔ Butterworth Stable
✔ زيادة order تزيد sharpness
✔ IIR يحتاج حذر

---

لو تحب أعمل مقارنة عملية بين FIR و IIR بنفس cutoff ونشوف الفرق في الطيف نقارنهم جنب بعض قولّي 👌


