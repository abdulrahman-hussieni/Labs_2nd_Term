
# توليد النغمات الصوتية باستخدام MATLAB/Octave

يوضح هذا المشروع كيفية إنشاء **نغمة صوتية نقية (Pure Tone)** برمجياً عن طريق توليد موجة جيبية (Cosine Wave) وتحويلها إلى إشارة صوتية مسموعة.

## الكود المستخدم (The Code)

```matlab
% إعدادات المعاملات
% Parameter setup
fs = 8000;              % Sampling Frequency (Hz)
time = 1;               % Duration (seconds)
f0 = 500;               % Tone frequency (Hz)

% Time vector
t = 0:(1/fs):time;

% Generate cosine wave
% 0.1 is used to reduce amplitude and avoid clipping
x = 0.1 * cos(2 * pi * f0 * t);

% Play sound
sound(x, fs, 16);

```

---

## شرح المكونات التقنية

### 1. لماذا اخترنا الحدود من 0 إلى 1؟

تمثل هذه الحدود **الفترة الزمنية** للإشارة.

* الرقم `0` هو لحظة البداية.
* الرقم `1` هو لحظة النهاية (ثانية واحدة).
* **الدقة:** بما أن التردد `fs` هو 8000، فإن البرنامج يقوم بإنشاء 8000 نقطة حسابية داخل هذه الثانية الواحدة لضمان سلاسة الصوت عند تحويله من صيغة رقمية إلى تماثلية (Digital to Analog).

### 2. لماذا دالة جيب التمام `cos`؟

تعتبر دالة `cos` (أو `sin`) هي الوحدة البنائية الأساسية للصوت في الفيزياء:

* **النقاء:** تنتج دالة الكوساين ما يسمى بالـ "Simple Harmonic Motion"، وهو ما نسمعه كـ "نغمة نقية" بدون ضجيج.
* **التردد:** المعادلة المستخدمة هي ، حيث  هو التردد الذي يحدد حدة الصوت (Pitch). كلما زاد هذا الرقم، أصبح الصوت "أرفع" أو أكثر حدة.

### 3. سعة الصوت (Amplitude)

لاحظ استخدام معامل الضرب `0.1`. هذا المعامل يتحكم في **شدة الصوت**:

* القيم الافتراضية لـ `cos` تتراوح بين  و .
* استخدام القيمة القصوى () قد يؤدي إلى ظاهرة الـ **Clipping** (تشوه الصوت) إذا كانت سماعات الجهاز غير مهيأة، لذا نستخدم  كقيمة آمنة ومريحة للأذن.

---
📐 Nyquist Criterion (The Golden Rule)

To reconstruct a signal correctly:

fs > 2 * f0

Why?
✔ Prevent Aliasing

If sampling frequency is too low, the signal becomes distorted.

✔ Perfect Reconstruction

Enough samples ensure accurate waveform rebuilding..


انشاء موجة مركبة 
```matlab
 % x = A*cos(2*pi*f0*t)

Fs = 8000;
time = 1;
t = 0:(1/Fs):time;

x = 0.1*cos(2*pi*300*t) + 0.1*cos(2*pi*400*t);

sound(x, Fs, 16);


```
رسم الموجة
:

```matlab
% x = A*cos(2*pi*f0*t)

Fs = 8000;
time = 1;

t = 0:(1/Fs):time;

x = 0.1*cos(2*pi*300*t);

sound(x,Fs,16);

figure(1);plot(t,x),grid;
xlabel('Time')
ylabel('Amplitude')
title('Time domain representation of x(t)')
xlim([0,0.1])
```
رسم موجة عشوائية(random signal uniformly distributed):
:

```matlab
Fs = 8000;                 % تردد أخذ العينات (عدد العينات في الثانية)

time = 1;                  % مدة الإشارة بالثواني

N = Fs*time;               % إجمالي عدد العينات = التردد × الزمن

x = rand(1,N)*2-1;         % توليد إشارة عشوائية بين -1 و +1 (ضوضاء بيضاء)

t = linspace(0,time,N);    % إنشاء متجه الزمن من 0 إلى 1 بعدد N من النقاط

sound(x,Fs,16);            % تشغيل الإشارة الصوتية بدقة 16-bit

figure(1);                 % فتح نافذة رسم رقم 1

plot(t,x),grid;            % رسم الإشارة مقابل الزمن مع إظهار الشبكة

xlabel('Time')             % تسمية المحور الأفقي (الزمن)

ylabel('Amplitude')        % تسمية المحور الرأسي (السعة)
```
